package deadlock;

public class DeadLock {

	/*
	 * 		Deadlock is a situation where multiple thread are waiting for each other, therefore all threads are blocked
	 * 
	 * 		deadlock is caused by SYNCHRONIZATION
	 * 
	 * 		the synchronized word caused thread to block, while waiting for each other 
	 */
	
}
